name: image-search
channels:
  - conda-forge
  - pytorch
  - defaults

dependencies:
  - python=3.10.*
  - pip>=21.3
  - mypy>=1.10,<2.0
  - ruff>=0.6,<1.0
  - pytest>=8.0,<10.0.0
  - pytest-rerunfailures>=14.0,<17.0
  - pytest-timeout>=2.3,<3.0
  - pre-commit>=3.0,<5.0.0
  # Khai báo gói PyPI tại đây.
  - pip: []
    # Ví dụ:
    # - pip:
    #     - requests>=2.32.0
    #     - python-dotenv>=1.0.1
